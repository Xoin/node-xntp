var Core = require('../includes');

function Log(type, params) {
    switch (type) {
        case "server":
            console.log(params);
            break;
        case value:
            break;

        default:
            break;
    }
}

module.exports = {
    Log: Log
}