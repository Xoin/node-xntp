var Patterns = {
    977:{
        200:/^(ARTICLE) ([0-9]+)$/g,
        201:/^(ARTICLE) ([0-9])$/g
    },
    3978:{
        "arctile":/^(ARTICLE) ([0-9]+)$/g
    }
}

module.exports = {
    Patterns
};