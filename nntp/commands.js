var includes = require('../includes');

module.exports = {
    Parser
};

function Parser(conn, sockets, command) {
    var text = "ARTICLE 20";
    var found = regex[3977][201].exec(text)


    console.log(found)
}